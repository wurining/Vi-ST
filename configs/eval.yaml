# @package _global_

defaults:
  - _self_
  - dataset: task.yaml
  - model: down_stream_module.yaml
  - logger: null
  - trainer: gpu.yaml
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
